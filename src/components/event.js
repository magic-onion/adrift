import React, { Component } 'react'

export default class Event extends Component {
  constructor(props) {
    super(props)
    this.state = {
      events: []
    }
  }


}


//do we really want to do deeply nested state in a deeply nested component to handle core functionality? lul
